# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
    #config.vm.box = "generic/ubuntu1804"
    #config.vm.define 'VPS_DEV'
    #config.ssh.pty = true
    #config.vm.provider "virtualbox" do |vb|
	#	vb.customize ["modifyvm", :id, "--graphicscontroller", "vmsvga"]
	#	vb.customize ["modifyvm", :id, "--nested-hw-virt", "on"]
	#	vb.customize ["modifyvm", :id, "--nestedpaging", "on"]
	#	vb.name		= "VPS_DEV"
	#	vb.memory	= "4096"
	#	vb.cpus   	= "3"
	#end
    #config.vm.synced_folder Dir.pwd, "/vagrant", disabled: false
   
    #config.vm.provision :shell, path: "vagrant-libs/bootstrap.sh"
    #config.vm.provision :shell, path: "requerimentos_vagrant.sh"

    #config.bindfs.bind_folder Dir.pwd, "/vagrant",
    #after: :provision,
    #perms: "u=rwX:g=r:o=r", # http://bindfs.org/docs/bindfs.1.html#sect12
    #create_as_user: true

	config.vm.define "VPS_DEV"
	config.vm.box = "generic/ubuntu1804"
	#config.vm.box_version = "0.4.4"
	#config.vm.hostname  = "vps"
	#config.ssh.username = "neo"
	#config.ssh.password = "neoricalex"
	#config.disksize.size = '100GB' 

	config.vm.provider "virtualbox" do |vb|
		vb.customize ["modifyvm", :id, "--graphicscontroller", "vmsvga"]
		vb.customize ["modifyvm", :id, "--nested-hw-virt", "on"]
		vb.customize ["modifyvm", :id, "--nestedpaging", "on"]
		vb.name		= "VPS_DEV"
		vb.memory	= "4096"
		vb.cpus   	= "3"
	end

	#config.vm.synced_folder Dir.pwd, "/vagrant", disabled: false
	#config.bindfs.bind_folder Dir.pwd, "/vagrant",
    #perms: "u=rw:g=r:o=r", # http://bindfs.org/docs/bindfs.1.html#sect12
    #create_as_user: true

	config.vm.provision :shell,
		path: "vagrant-libs/bootstrap.sh"

	#config.vm.provision :shell do |shell|
	#	begin
	#		shell.path = "vagrant-libs/bootstrap.sh"
	#	rescue
	#		shell.privileged = true
	#		shell.inline = 'echo rebooting'
	#		shell.reboot = true
	#	end
	#end
end
  